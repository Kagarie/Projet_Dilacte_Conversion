/*Debut du parser et fin */
//case YAML_STREAM_START_TOKEN: puts("STREAM START"); break;
//case YAML_STREAM_END_TOKEN:   puts("STREAM END");   break;

/*Affichage du token avant de changer sa valeur*/
//case YAML_KEY_TOKEN:   printf("(Key token)   "); break;
//case YAML_VALUE_TOKEN: printf("(Value token) "); break;
                
/*les deux premiers case marque le début est la fin de chaque bloque dans le fichiers yaml !*/
/*case YAML_BLOCK_SEQUENCE_START_TOKEN: puts("<b>Start Block (Sequence)</b>"); break;
case YAML_BLOCK_ENTRY_TOKEN:          puts("<b>Start Block (Entry)</b>");    break;
case YAML_BLOCK_END_TOKEN:            puts("<b>End block</b>");              break;
*/

/* Data */
//case YAML_BLOCK_MAPPING_START_TOKEN:  puts("[Block mapping]");            break;
